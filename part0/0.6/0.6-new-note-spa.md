# Exercise 0.6: New Note in Single Page App
**Name:** Muhammad Taha Nawab

```mermaid
sequenceDiagram
    participant browser
    participant server

    note right of browser: User writes text in input field and clicks Save button

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note left of server: Server creates new note object with content and timestamp
    server-->>browser: HTTP 201 Created { "message": "note created" }
    deactivate server

    note right of browser: Browser adds new note to the notes list in memory
    note right of browser: Browser re-renders the notes on the page
    note right of browser: No page reload happens (SPA behavior)